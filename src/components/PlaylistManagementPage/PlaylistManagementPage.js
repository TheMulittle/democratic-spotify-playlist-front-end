import React from 'react'

const playlistManagementPage = (props) => (
    <React.Fragment>
        <div>Playlist Id Input</div>
        <div>Add button</div>
        <div hidden>Managed playlists</div>
        <div hidden>User's collaborative playlists</div>
    </React.Fragment>
)

export default playlistManagementPage;
